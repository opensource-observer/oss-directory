version: 7
name: reclaim-protocol
display_name: Reclaim Protocol
description: Reclaim protocol uses https session keys to generate zk proofs of user's
  profiles, enabling verifiable credentials without APIs.
